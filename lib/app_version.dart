/// Version affichée dans l'app (alignée sur pubspec.yaml).
class AppVersion {
  AppVersion._();

  static const String label = '1.4.2';
  static const int build = 5;
  static const String full = '$label+$build';
}
